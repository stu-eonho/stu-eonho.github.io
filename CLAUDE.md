# CLAUDE.md

## 하네스: Study Log 블로그 구축 (Astro 7 정적 사이트 + 잔잔한 모션)

**목표:** `STUDY_LOG_SPEC.md`와 `DESIGN.md`를 근거로 개인 연구·학습 기록 블로그를 구축하고, anime.js v4로 과하지 않은 진입 모션을 입힌다.

**트리거:** 이 블로그의 구현·수정·검증 관련 요청 시 `study-log-build` 스킬을 사용하라. 초기 구축과 후속 작업(부분 수정, 재실행, 모션 조정) 모두 이 스킬을 거친다. 스펙 내용 확인이나 파일 위치 문의 같은 단순 질문은 직접 응답 가능.

**참조 전용 파일:** `DESIGN.md`는 수정하지 않는다. 토큰을 바꿔야 하면 `src/styles/global.css`에 `[local extension]` 주석과 함께 확장 토큰을 추가한다.

**다국어:** 사이트는 한국어(`/`)와 영어(`/en/`) 두 벌로 빌드된다. 화면 문자열을 컴포넌트에 직접 쓰지 않는다 — 전부 `src/config/i18n.ts`의 `UI` 사전을 거친다. 내부 링크는 `i18n(Astro.url).href()`를 거친다. 계약은 `_workspace/06_i18n_contract.md`.

**배포:** `stu-eonho.github.io` (GitHub Pages 사용자 사이트, Actions 배포).

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-21 | 초기 구성 — 에이전트 6종, 스킬 5종 | 전체 | - |
| 2026-08-21 | 모션 레이어 추가 | agents/motion-designer.md, skills/subtle-motion-anime | 사용자 요청: anime.js 기반 잔잔한 애니메이션. 스펙 `<animations>`의 CRITICAL 제약(페이지 전환 없음, reduced-motion, JS 0바이트 기본값)은 유지하고 진입 리빌 레이어만 추가 |
| 2026-08-21 | 초기 구축 완료 | 프로젝트 전체 (`src/`, `scripts/`, `.github/`, `README.md`, `_workspace/`) | 스펙 전 범위 구현. `astro check` 0오류/0경고, 19페이지 빌드, 내부 링크 493건 이상 없음, 대비 38조합 AA 통과 |
| 2026-08-21 | 토큰 5건 조정 | `src/styles/global.css` | WCAG AA 미달 해소. `--accent-hover` `#0077ed`→`#006edb`, `--accent-active` `#006edb`→`#0064c8`, `--accent-tint` 0.10→0.07, `--interest-tint` 0.08→0.05, `--disabled-fg` 신규. 전부 `[local extension]`이며 `[verified]` 값은 무변경 |
| 2026-08-21 | 모션 트리거 방식 변경 | `src/scripts/motion.ts` | anime.js `onScroll` 대신 IntersectionObserver 직접 사용. 청크 gzip 20.2KB→14.9KB. 스킬 예산 12KB는 여전히 미달성 — anime.js v4 엔진 하한이 약 15.9KB이며 12KB 이하는 라이브러리 미사용으로만 가능 (`_workspace/04_motion_inventory.md` 참조) |
| 2026-08-21 | 모션 깜빡임 수정 | `src/styles/motion.css`, `src/scripts/motion.ts`, `ProfileHero`, `ListHeader`, `PostMetaHeader`, 메타 패널 2종 | 사용자 피드백: 진입 애니메이션이 뒤늦게 걸려 깜빡임. 원인은 이미 그려진 요소를 anime.js 도착 후 숨겼다 다시 보여준 것. 리빌을 CSS(`data-reveal-onload`, 첫 페인트)와 JS(`data-reveal`, 스크롤 진입) 두 갈래로 분리하고, 스크립트 시작 시점에 화면에 보이는 요소는 JS 대상에서 제외 |
| 2026-08-21 | 홈 상단 여백 축소 | `src/components/home/ProfileHero.astro` | 사용자 피드백: 위쪽 공백 과다. 히어로 padding-top 56→32px(모바일) / 80→56px(데스크톱). 하단과 섹션 리듬은 유지 |
| 2026-08-21 | 홈을 CV 형식으로 재구성 | `ProfileCard`(신규), `TimelineList`(신규), `CareerList`(신규), `EducationList`·`SkillGroups`·`InterestList`·`index.astro` 수정, `ProfileHero` 삭제 | 사용자 피드백: 홈이 흩어져 보임. 프로필 관련 요소를 Card 하나에 모으고 hairline으로만 구분. "Paper Review 보기" CTA 제거. 스펙 `home_page`의 히어로+분리 섹션 레이아웃을 대체하되 토큰(색·radius·폰트 스케일·44px 히트 영역)은 그대로 준수 |
| 2026-08-21 | 프로필에 경력 필드 추가 | `src/config/profile.ts`, `CareerList.astro`, `src/lib/seo.ts`, README | 사용자 요청. `CareerEntry`(company/role/team/employment/startDate/endDate/description) 추가, Person JSON-LD에 `worksFor`·`jobTitle` 반영 |
| 2026-08-21 | 플레이스홀더 표시 정책 변경 | `ProfileCard.astro` | 기존에는 꺾쇠 플레이스홀더를 화면에서 숨겨 홈이 비어 보였다. 이제 텍스트 플레이스홀더는 그대로 노출하고(무엇을 채울지 보이게) URL만 계속 필터링한다 |
| 2026-08-21 | 한국어/영어 이중 언어 도입 | `src/config/i18n.ts`(신규), `src/components/views/*`(신규 8종), `src/components/layout/LangToggle.astro`(신규), `src/components/post/TranslationNotice.astro`(신규), `src/lib/feed.ts`(신규), `src/pages/en/**`(신규 10종), `site.ts`·`categories.ts`·`profile.ts`·`content.config.ts`·`posts.ts`·`tags.ts`·`date.ts`·`seo.ts`·레이아웃·컴포넌트 다수 수정 | 사용자 요청: 해외 포트폴리오용 EN/KO 전환. URL 분리(`/` = ko, `/en/` = en) 방식으로 확정 — 클라이언트 토글은 검색엔진이 영어 페이지를 색인하지 못해 포트폴리오 목적을 달성하지 못한다. 전환 UI는 헤더 우측(검색·테마 토글 옆) 링크 1개이며 **JS 0바이트**. UI·프로필은 완전 이중언어, 글 본문은 `<slug>.en.mdx` 번역본이 있으면 영어·없으면 원문 + 안내 배너(canonical은 원문 URL로). 계약은 `_workspace/06_i18n_contract.md` |
| 2026-08-21 | 배포 대상 확정 | `astro.config.mjs`, `src/config/site.ts`, README §7 | 사용자가 `stu-eonho.github.io` 리포지토리를 생성. `site`/`url` 기본값을 `https://stu-eonho.github.io`로 변경. `base`는 `/` 유지(사용자 사이트) |
| 2026-08-22 | 로컬 전용 관리자 콘솔 추가 | `admin/`(신규 40여 파일), `astro.config.mjs`, `markdown.config.mjs`(신규), `src/content.config.ts`, `src/content/schema.ts`(신규), `src/styles/global.css`, `package.json`, `README.md`, `_workspace/07_admin_contract.md`(신규) | `ADMIN_CONSOLE_FEATURE_SPEC.md` 구현. `astro dev`에서만 열리는 `/admin` 8화면 + `/__admin/api/*` — 글 CRUD·EN 번역본·프로필·설정·카테고리·UI 문자열·태그·이미지. Zod 스키마를 `src/content/schema.ts` 팩토리로, 마크다운 설정을 `markdown.config.mjs` 팩토리로 추출해 빌드와 관리자가 같은 한 벌을 쓴다. 배제 4겹(디렉터리 배치 / `command === 'dev'` 게이트 / `configureServer` 전용 플러그인 / `astro:build:done` 가드)이며 가드 역검증 완료. `dist`는 도입 전과 바이트 동일(pagefind 인덱스 키 순서만 예외), `astro check` 0/0, JS gzip 17.3KB 무변화. `global.css`에 `@source not '../../admin'` 추가 — Tailwind가 관리자 소스에서 클래스 후보를 끌어와 프로덕션 CSS에 규칙을 싣는 것을 실측으로 확인했다 |
| 2026-08-23 | 관리자 편집기 자리표시 결함 3건 수정 + 링크 게이트 보강 | `admin/client/editor.ts`, `admin/pages/editor.astro`, `admin/pages/profile.astro`, `admin/labels.ts`, `admin/components/AdminLayout.astro`, `admin/styles/admin.css`, `admin/server/content.mjs`, `scripts/check-links.mjs`, `src/components/home/SkillGroups.astro` | 사용자 피드백. (1) 이미지·링크 도구가 `../../../assets/파일명`·`](url)` 자리표시를 본문에 넣어 각각 dev 서버 중단과 죽은 링크 배포를 일으켰다 — 둘 다 다이얼로그로 실제 값만 넣게 바꾸고, 저장 4번 관문에서 본문 이미지·링크 참조를 검사한다. (2) 관리자 프리뷰에 `katex.min.css`와 `prose.css`가 빠져 수식이 MathML+HTML 두 벌로 겹쳐 보였다 — 둘 다 싣고 프리뷰↔사이트 마크업 동등성을 실측 검증. (3) 스킬 그룹명이 비면 왼쪽 120px 칸이 빈 채로 남던 것을 `group-unnamed`로 해소. `check-links.mjs`가 상대 경로를 건너뛰어 `href="url"`을 놓치던 것도 함께 고쳤다(역검증 완료) |
| 2026-08-23 | 관리자에 git 커밋·푸시 추가 (스펙 Q1 번복) | `admin/server/handlers/git.mjs`, `admin/server/errors.mjs`, `admin/server/middleware.mjs`, `admin/client/dashboard.ts`, `admin/pages/index.astro`, `admin/labels.ts`, `_workspace/07_admin_contract.md` | 사용자 요청. 스펙 `open_questions` Q1의 기본값은 읽기 전용이었으나 사용자가 명시적으로 요청했고 초안 배포 위험을 인지·수용했다. `POST /git/commit`(파일 선택 + 메시지)과 `POST /git/push`(업스트림으로만) 추가. 인젝션 방어는 `execFile` 인자 배열이며 `x"; touch pwned` 메시지가 그대로 저장되고 파일이 생기지 않음을 실측했다. `--force` 경로는 서버·화면 어디에도 없고, 업스트림 없음/보낼 커밋 없음/원격이 앞섬을 각각 거절한다. 푸시는 확인 창(기본 취소)을 거치며 초안 글 수를 경고한다. 커밋·푸시도 기존 3중 게이트를 그대로 지난다(실측). 격리된 임시 저장소로 18건 검증 |
| 2026-08-23 | 빈 필수 필드가 콘텐츠 파일을 깨뜨리던 결함 수정 | `src/content/schema.ts`, `admin/server/codegen/frontmatter.mjs`, `admin/client/post-list.ts`, `admin/styles/admin.css` | 사용자 신고. 설명을 비운 채 저장하면 `description: ''`가 스키마를 통과하고 직렬화가 빈 값을 키째로 생략해 `description` 없는 파일이 남았다 — 다시 읽을 때 `Required`로 실패해 **dev 서버와 빌드가 함께 죽었고, 관리자가 dev 서버 안에 살기 때문에 고칠 도구조차 열리지 않는 교착**이 됐다. 스키마의 `title`·`description`에 `.trim().min(1)`을 넣어 단일 진실 공급원이 거절하게 하고(공백만 있는 값은 `.min(1)`을 통과하므로 `.trim()`이 앞에 와야 한다), 직렬화는 `REQUIRED_KEYS`를 값이 비어도 남기도록 했다. 목록의 오류 배지가 메시지를 툴팁에만 담던 것도 행 안에 직접 표시하도록 고쳤다 |
| 2026-08-23 | 본문 이미지 표시 크기 조절 추가 + 프리뷰 이미지 404 수정 | `markdown.config.mjs`, `admin/pages/editor.astro`, `admin/client/editor.ts`, `admin/labels.ts`, `admin/server/handlers/preview.mjs`, `README.md` | 사용자 요청. 두 해석(표시 크기 / 업로드 원본 축소)을 물어 **표시 크기, 픽셀 직접 입력**으로 확정했다. `![설명|400](경로)` 표기를 `markdown.config.mjs`의 `rehypeImageSize`가 읽어 `max-width: min(400px, 100%)`로 옮긴다 — 빌드와 프리뷰가 같은 팩토리를 쓰므로 자동으로 일치한다. Astro가 넣은 `width`/`height`는 CLS 근거라 덮어쓰지 않고, 고정 px가 좁은 화면을 넘지 않도록 `min()`을 쓴다. 곁다리로 **프리뷰의 본문 이미지가 404로 깨지던 것**을 고쳤다 — 저장 경로 `../../../assets/x`가 `/admin/posts/edit` 기준으로 `/assets/x`가 되어 dev 서버가 서빙하지 않는 자리였다. 프리뷰 렌더 결과의 `src`만 `/src/assets/…`로 바꾸고 저장 본문은 건드리지 않는다 |
| 2026-08-26 | 본문 이미지 정렬(왼쪽·가운데·오른쪽) 추가 | `markdown.config.mjs`, `admin/pages/editor.astro`, `admin/client/editor.ts`, `admin/labels.ts`, `README.md` | 사용자 요청. 기존 `|400` 너비 표기를 **옵션 목록**으로 확장했다 — `![설명|400|center]`처럼 순서 무관하게 붙는다. 파서는 뒤에서부터 아는 토큰(숫자 또는 left·center·right)인 동안만 떼어 내고 모르는 조각에서 멈추므로 `![A | B]` 같은 평범한 문장은 그대로 둔다. 정렬은 `float`가 아니라 블록 정렬이며(글이 감싸고 흐르지 않는다), 이미지가 인라인이라 `display: block`을 함께 넣어야 `margin: auto`가 듣는다. 편집기 이미지 삽입 창에 정렬 선택이 생겼다. 18건 검증 |
| 2026-08-26 | `description`을 선택 필드로 전환 | `src/content/schema.ts`, `admin/server/codegen/frontmatter.mjs`, `PostCard.astro`, `PostMetaHeader.astro`, `src/lib/seo.ts`, `admin/labels.ts`, `admin/pages/editor.astro`, `README.md` | 사용자 요청: 설명 입력을 계속 요구받는 것이 번거롭다. 2026-08-23에 필수로 만든 이유는 **빈 값이 파일을 깨뜨리는 것**이었지 설명 없는 글이 불가능해서가 아니었다 — 필드가 선택이 되면 "키가 없어 읽기 실패"라는 사슬 자체가 성립하지 않으므로 그때의 안전성을 잃지 않는다. `.min(1)` 대신 `.optional()` + `transform`으로 빈 문자열·공백을 `undefined`로 접어 "없음"의 표현을 하나로 만들었다(그러지 않으면 빈 `<p>`와 빈 OG 설명이 남는다). 설명이 없으면 카드·글 머리의 설명 줄이 빠지고 OG·meta·RSS는 사이트 기본 설명으로 대체된다. `title`은 그대로 필수 |
| 2026-08-26 | 논문 리뷰 카드에 원 논문 인용 줄 추가 | `src/lib/paper.ts`(신규), `src/components/post/PostCard.astro`, `src/components/post/PaperMetaPanel.astro`, `README.md` | 사용자 요청: 목록 카드의 설명 자리에 저자·학회·연도가 나오면 좋겠다. `paper` 블록이 있는 글이면 제목 아래에 `저자들 · 학회 연도`를 낸다 — 판정을 카테고리가 아니라 **블록 존재**로 둔 이유는 categories.ts가 늘어날 수 있고 논문 메타를 가진 카테고리가 하나뿐이라는 보장이 없어서다. 저자 축약 규칙이 카드와 패널에 이중 구현되지 않도록 `src/lib/paper.ts`로 뽑았다(카드 3명, 패널 4명 — 카드는 약 360px 칸이라 더 좁게 줄인다). 곁다리로 **패널의 `lang="en"` 오적용**을 고쳤다 — `David Ha 외 1명` 전체를 감싸고 있어 스크린리더가 "외 1명"을 영어로 읽었다. 이제 이름·학회명에만 `lang`을 걸고 축약 문구는 밖에 둔다. 설명을 함께 쓰면 인용 줄 아래에 나온다(대체가 아니라 추가) |
| 2026-08-26 | dev 서버 재시작 규칙 문서화 | `admin/integration.mjs`(주석), `README.md` §2, `_workspace/07_admin_contract.md` §19 | 사용자 신고: 설명을 지우고 저장해도 옛 값이 화면에 남는다. 코드 결함이 아니라 **한 프로세스 안에서 세 벌의 코드가 섞여 돈 것**이었다 — 관리자 검증기(`ssrLoadModule`)는 새 스키마, 직렬화기(Node `import()` 캐시)는 옛 코드, 콘텐츠 컬렉션은 기동 시점의 옛 스키마. `admin/server/**`와 `src/content/schema.ts`는 파일 저장으로 갱신되지 않고 Astro/Vite 자동 재시작도 같은 프로세스라 Node 캐시를 비우지 못한다(pid 동일로 실측). `addWatchFile`로 감시해 봤으나 같은 이유로 무효라 되돌렸다. 고칠 수단이 없어 문서화로 마무리했고, `configureServer`의 "재시작 없이 반영된다"는 **틀린 주석**을 바로잡았다 — 그 주석이 원인 추적을 늦췄다. 껐다 켠 서버에서 저장 왕복 2회 정상 확인 |
| 2026-08-26 | 수식 깨짐 수정 — `math` 플래그를 본문에서 자동 판정 | `markdown.config.mjs`, `src/components/views/PostDetailView.astro`, `src/layouts/PostLayout.astro`, `admin/pages/editor.astro`, `admin/labels.ts`, `README.md` | 사용자 신고: 일반 글 화면에서 수식이 `P(z_t+1|a_t,z_t,h_t)P (zt+1 | at, zt, ht)`처럼 두 벌로 겹쳐 보인다(관리자 프리뷰는 정상). 원인은 KaTeX가 접근성을 위해 MathML+HTML 두 벌을 내고 그중 하나를 `katex.min.css`가 숨기는데, `BaseLayout`이 프론트매터 `math: true`일 때만 그 CSS를 실었기 때문 — 본문에 수식을 쓰고 체크를 안 하면 조용히 깨진다. 관리자는 `AdminLayout`이 CSS를 무조건 싣기에 멀쩡했다. **본문이 진실이므로 본문에서 읽도록** `remarkDetectMath`를 추가해 math/inlineMath 노드가 있으면 `frontmatter.math`를 켜고, `render()`의 `remarkPluginFrontmatter`로 받아 프론트매터 값과 OR한다. `math` 필드는 수동 강제용으로 남긴다(하위 호환). 수식 없는 글은 여전히 CSS를 싣지 않아 23KB 절약은 유지 |
