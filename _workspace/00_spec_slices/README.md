# 스펙 슬라이스

이번 실행은 오케스트레이터가 스펙 전문(1554줄)과 `DESIGN.md`를 직접 읽고 단일 컨텍스트에서 구현했다.
따라서 에이전트별 슬라이스 파일 5종은 만들지 않았고, 대신 전 모듈이 공유하는
`CRITICAL_REGISTRY.md` 하나를 남긴다. 후속 실행에서 부분 재작업을 할 때는
`CRITICAL_REGISTRY.md` + 해당 모듈의 계약 파일(`../01`~`../04`)을 읽으면 된다.
