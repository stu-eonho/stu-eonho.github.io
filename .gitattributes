# 리포지토리에는 항상 LF로 저장한다. 빌드는 Ubuntu 러너에서 돈다 —
# CRLF가 섞여 들어가면 diff가 파일 전체로 부풀고 셸 스크립트가 깨진다.
* text=auto eol=lf

# 바이너리 — 줄바꿈 변환 대상이 아니다
*.woff2 binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.ico   binary
