---
# 이 파일을 src/content/posts/paper-review/<슬러그>.mdx 로 복사해 새 글을 시작하세요.
# 밑줄(_)로 시작하는 파일은 컬렉션에서 제외되므로 이 템플릿 자체는 사이트에 나오지 않습니다.
slug: my-paper-slug # URL 세그먼트. 소문자·숫자·하이픈만. 슬래시 금지
# lang: ko                   # 생략하면 한국어입니다.
#   영어판을 쓰려면 이 파일을 <슬러그>.en.mdx 로 복사하고 lang: en 을 넣으세요.
#   slug은 원문과 **똑같이** 둡니다 — 그래야 두 글이 같은 글의 번역본으로 묶입니다.
#   영어판이 없으면 /en/ 에서는 이 원문이 그대로 나오고 안내 배너가 붙습니다.
title: 논문 제목을 한국어로 요약해 적습니다
description: 카드·검색·RSS에 함께 쓰이는 두 줄 요약입니다. 200자 이내.
category: paper-review
date: 2026-08-21
# updated: 2026-08-25       # 수정일. 있으면 상세 헤더에 "수정: ..."이 붙습니다
tags: [transformer, attention]
draft: true # 공개할 준비가 되면 false로 바꾸거나 줄을 지웁니다
math: true # 수식을 쓰는 글에만 true — KaTeX CSS가 이 글에만 실립니다
toc: true # false면 목차 컬럼을 숨기고 본문을 가운데 정렬합니다

# CRITICAL: paper-review 글에는 paper 블록이 반드시 있어야 합니다. 없으면 빌드가 실패합니다.
paper:
  paperTitle: Original Paper Title (원문 그대로, 번역하지 않습니다)
  authors: [First Author, Second Author]
  venue: NeurIPS
  year: 2025
  # arxivId: '2301.00001'    # https://arxiv.org/abs/{id} 링크가 생성됩니다
  # doi: 10.1000/example     # https://doi.org/{doi} 링크가 생성됩니다
  # pdfUrl: https://example.com/paper.pdf   # 외부 링크만 저장합니다 (PDF를 리포에 올리지 않습니다)
  # codeUrl: https://github.com/example/repo
  # readDate: 2026-08-20
---

## 한 줄 요약

이 논문이 무엇을 주장하는지 한 문장으로 적습니다.

## 배경

$$
\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right)V
$$

## 방법

```python
def scaled_dot_product_attention(q, k, v):
    scores = q @ k.transpose(-2, -1) / (k.size(-1) ** 0.5)
    return scores.softmax(dim=-1) @ v
```

## 한계와 생각

읽으면서 걸린 부분, 재현해 보고 싶은 실험을 적습니다.
